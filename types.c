#include <stdio.h>
#include <stdlib.h>


int main(int argc, char *argv[]) {
    float a = 4;
    float b = 3;
    int c = a / b;
    printf("Float %f entre float %f, asignado a un int, da: %d\n", a, b, c);
    
    return 0;
}